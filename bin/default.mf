// this file default.fmf will be args default at launch the executable.
MANIFEST 1

// title of window at launch
NAME "A game"
BASE_GAME CBase
GAME_DIR cbase
// 1: is home dir at launch
// 0(default): is not home dir, only mod(requires set game arg)
ENABLE_HOMEDIR 1

// engine will recognize all pak files from game/mod dir