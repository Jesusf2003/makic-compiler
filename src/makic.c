#include "makic.h"

#include <stdlib.h>

static const char *app_name;

/// Starts program
int main(int argc, char **argv) {
    bool has_progs = false;
    bool succeed = false;
    FILE *outfile = nullptr;
    app_name = argv[0];

    // init opts
    return 0;
}